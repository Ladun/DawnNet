syntax = "proto3";
package Protocol;

message C_LOGIN
{
	
}

message S_LOGIN
{
	bool success = 1;
	repeated int32 players = 2;
}

message C_CHAT
{
	string nickname = 1;
	string msg = 2;
}

message S_CHAT
{
	uint64 playerId = 1;
	string nickname = 2;
	string msg = 3;
}