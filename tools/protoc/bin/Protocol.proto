syntax = "proto3";
package Protocol;

message C_LOGIN
{
	
}

message S_LOGIN
{
	bool success = 1;
	repeated int32 players = 2;
}

message C_CHAT
{
	string msg = 1;
}

message S_CHAT
{
	uint64 playerId = 1;
	string msg = 2;
}